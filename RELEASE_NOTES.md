# WebPDF リリースノート

---

## バージョン 1.3（最新）

**リリース日：2026年3月**

### 変更点

- Android 15（API 35）への正式対応
- コード圧縮（R8）とリソース縮小の有効化によるアプリサイズの削減
- ビルド安定性の向上

---

## バージョン 1.0〜1.2 の主な機能

以下は現在のバージョンで利用できる機能の一覧です。

---

### ブラウザ・URL管理

**ブラウザ内蔵**
アプリ内でそのままWebページを開けます。URLを入力するか、キーワードでGoogle検索もできます。

**URLの保存とリスト管理**
気になったページのURLをブックマークとして保存できます。保存時にはページタイトルを自動取得して表示。保存日時も記録されるので、あとから見返しやすくなっています。個別削除と一括削除に対応。

**他アプリからの共有受け取り**
ChromeやSafariなどで開いているページを「共有」メニューからWebPDFに送ると、URLが自動でリストに保存されます。

---

### PDFの保存

**現在のページをPDF保存**
表示中のページをPDFとして保存できます。ファイル名はページタイトルから自動生成。保存先はダウンロードフォルダ（`Downloads/WEB_PDF`）か、設定で指定した任意のフォルダ（Google Driveを含む）を選べます。

**複数リンクのまとめてPDF保存（一括保存）**
ページ上でなぞるようにドラッグして範囲を選択すると、その範囲内のリンクを抽出できます。抽出したリンクをまとめてPDFに変換・保存する一括保存機能に対応。保存中はステータスバーに進捗が表示されます。

---

### ページ編集機能

PDF保存前にページの見た目を整えられます。右下のメニューボタンから操作します。

| 機能 | 内容 |
|---|---|
| 広告を削除 | ページ内の広告要素をまとめて非表示 |
| 記事下部を削除 | 関連記事・コメント欄など記事末尾の不要な部分を一括削除 |
| 要素を削除 | 画面上の任意の要素をタップして個別に削除。やり直し（Undo）にも対応 |
| 文字のみ表示 | 画像や装飾を非表示にしてテキストだけを表示 |
| 白黒モード | ページをグレースケールに変換 |
| 背景を削除 | 背景色・背景画像を非表示 |
| 画像調整 | 画像を画面幅に合わせて自動フィット |

---

### 広告ブロック

**ネットワークレベルのブロック**
Google・Meta・Yahoo! JAPAN・TikTok・Amazon・Xなど主要広告ネットワークへのリクエストを、ページ読み込み時点でブロックします。「強力な広告ブロック」設定をオンにすると、さらに幅広い広告ネットワークをまとめてブロックします。

---

### プリセット

よく使う編集設定（広告削除・文字のみ・グレースケール・背景削除・画像調整・削除した要素のセレクター）を名前をつけて保存できます。次回以降はワンタップで同じ設定を再現。一括保存時にもプリセットを選んで適用できます。

---

### サイトごとの最適化設定

ドメイン単位で動作を細かく設定できます。

- **表示モード**：モバイル表示 / PC表示を切り替え（グローバル設定の上書き）
- **スティッキーメニュー対策**：スクロールしても追従するメニューバーを固定解除
- **遅延読み込みのスキップ**：スクロールに連動して読み込む画像の強制展開をオフにしたい場合
- **自動再生の許可**：動画の自動再生を許可するサイト
- **広告ブロックの除外**：特定サイトのみブロックを無効化（ホワイトリスト）
- **カスタムCSS / JavaScript**：サイトに対して独自のスタイルやスクリプトを自動注入

---

### アプリ設定

| 設定項目 | 内容 |
|---|---|
| 強力な広告ブロック | より広範囲の広告ネットワークをネットワークレベルでブロック |
| メニュー修正 | スティッキーメニューを全サイトに対してまとめて解除 |
| 画像の自動調整 | ページ読み込み時に画像を自動フィット |
| PCモード | デスクトップ向けUser-Agentで全サイトを表示 |
| メニューカスタマイズ | 右下メニューに表示するアクションを個別に選択 |
| 一括保存先 | PDFの保存先フォルダを変更（Google Drive対応） |
| プリセット管理 | 保存済みプリセットの確認・削除 |

---

## 今後の予定

- 操作フローの改善
- 保存済みPDFの閲覧・管理機能の強化
- フィードバックをもとにした細かな使い勝手の改善

ご意見・不具合報告はこちらへ：**iam_buridaikon@rakumail.jp**

---

*開発者：原マルチノ　／　X: @h2LiyIYxWl13987*
